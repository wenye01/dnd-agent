# v0.2 前端实现指南 - UI组件+聊天+面板+布局

> **版本**: v0.2
> **前置**: v0.1 (基础设施)
> **目标**: 实现基础 UI 组件、聊天界面、角色面板、主界面布局、Phaser 集成

---

## 版本目标

完成本版本后，前端可以：
1. 显示游戏主界面（三栏布局）
2. 通过聊天界面与 DM 交互
3. 在角色面板显示角色信息
4. 运行 Phaser 游戏引擎（P1）

---

## 迭代 0.2.1 - 基础 UI 组件

### 需求清单

- [x] 通用 UI 组件
- [x] 布局组件
- [x] 样式系统

### 实现指导

#### 1. Button 组件

```tsx
// src/components/ui/Button.tsx

import { forwardRef } from 'react'

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'danger' | 'ghost'
  size?: 'sm' | 'md' | 'lg'
  loading?: boolean
}

export const Button = forwardRef<HTMLButtonElement, ButtonProps>(
  ({ variant = 'primary', size = 'md', loading, children, className, disabled, ...props }, ref) => {
    const baseStyles = 'font-display font-semibold rounded transition-colors focus:outline-none focus:ring-2'

    const variantStyles = {
      primary: 'bg-primary-500 text-white hover:bg-primary-600 focus:ring-primary-300',
      secondary: 'bg-gray-200 text-gray-800 hover:bg-gray-300 focus:ring-gray-200',
      danger: 'bg-red-500 text-white hover:bg-red-600 focus:ring-red-300',
      ghost: 'bg-transparent text-gray-600 hover:bg-gray-100 focus:ring-gray-200',
    }

    const sizeStyles = {
      sm: 'px-2 py-1 text-sm',
      md: 'px-4 py-2 text-base',
      lg: 'px-6 py-3 text-lg',
    }

    const disabledStyles = 'opacity-50 cursor-not-allowed'

    return (
      <button
        ref={ref}
        className={`
          ${baseStyles}
          ${variantStyles[variant]}
          ${sizeStyles[size]}
          ${disabled || loading ? disabledStyles : ''}
          ${className || ''}
        `}
        disabled={disabled || loading}
        {...props}
      >
        {loading ? (
          <span className="flex items-center gap-2">
            <span className="animate-spin">⏳</span>
            Loading...
          </span>
        ) : (
          children
        )}
      </button>
    )
  }
)

Button.displayName = 'Button'
```

#### 2. Panel 组件

```tsx
// src/components/ui/Panel.tsx

import { ReactNode } from 'react'

interface PanelProps {
  title?: string
  children: ReactNode
  className?: string
  onClose?: () => void
}

export function Panel({ title, children, className, onClose }: PanelProps) {
  return (
    <div className={`bg-parchment border-2 border-ink rounded-lg shadow-lg ${className || ''}`}>
      {title && (
        <div className="flex items-center justify-between px-4 py-2 border-b border-ink">
          <h3 className="font-display text-lg font-bold text-ink">{title}</h3>
          {onClose && (
            <button
              onClick={onClose}
              className="text-ink hover:text-primary-500 transition-colors"
            >
              ✕
            </button>
          )}
        </div>
      )}
      <div className="p-4">{children}</div>
    </div>
  )
}
```

#### 3. Input 组件

```tsx
// src/components/ui/Input.tsx

import { forwardRef } from 'react'

interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  label?: string
  error?: string
}

export const Input = forwardRef<HTMLInputElement, InputProps>(
  ({ label, error, className, ...props }, ref) => {
    return (
      <div className="flex flex-col gap-1">
        {label && (
          <label className="font-display text-sm font-semibold text-ink">
            {label}
          </label>
        )}
        <input
          ref={ref}
          className={`
            px-3 py-2 border-2 border-ink rounded
            bg-white text-ink font-body
            focus:outline-none focus:ring-2 focus:ring-primary-300
            ${error ? 'border-red-500' : ''}
            ${className || ''}
          `}
          {...props}
        />
        {error && (
          <span className="text-sm text-red-500">{error}</span>
        )}
      </div>
    )
  }
)

Input.displayName = 'Input'
```

### 预期效果

```tsx
// 使用示例
<Button variant="primary" onClick={handleClick}>
  开始游戏
</Button>

<Panel title="角色信息">
  <p>内容</p>
</Panel>
```

### 验收标准

```bash
# 1. 组件渲染测试
npm run storybook  # 如果配置了 Storybook

# 2. 样式一致性
# - 所有组件使用统一主题色
# - 字体风格一致
```

### 文档资料

- [UI 组件设计](../modules/frontend/components/ui-components.md)

---

## 迭代 0.2.2 - 聊天界面

### 需求清单

- [x] 消息列表展示
- [x] 消息输入框
- [x] 流式叙述显示
- [x] 消息发送逻辑

### 实现指导

#### 1. 聊天容器

```tsx
// src/components/chat/ChatContainer.tsx

import { useEffect, useRef } from 'react'
import { useChatStore } from '../../stores/chatStore'
import { useWebSocket } from '../../contexts/WebSocketContext'
import { MessageList } from './MessageList'
import { MessageInput } from './MessageInput'
import { StreamingNarration } from './StreamingNarration'

export function ChatContainer() {
  const { connected, send, subscribe } = useWebSocket()
  const {
    messages,
    streamingText,
    isStreaming,
    addUserMessage,
    appendStreamText,
    finalizeStreamText,
  } = useChatStore()

  const containerRef = useRef<HTMLDivElement>(null)

  // 订阅 WebSocket 消息
  useEffect(() => {
    return subscribe((message) => {
      switch (message.type) {
        case 'narration': {
          const payload = message.payload as { text: string; isStreaming: boolean }
          if (payload.isStreaming) {
            appendStreamText(payload.text)
          } else {
            finalizeStreamText()
          }
          break
        }
        case 'state_update': {
          // 处理状态更新
          break
        }
        case 'dice_result': {
          // 处理骰子结果
          break
        }
      }
    })
  }, [subscribe])

  // 自动滚动到底部
  useEffect(() => {
    if (containerRef.current) {
      containerRef.current.scrollTop = containerRef.current.scrollHeight
    }
  }, [messages, streamingText])

  const handleSend = (text: string) => {
    addUserMessage(text)
    send({
      type: 'user_input',
      payload: { text },
    })
  }

  return (
    <div className="flex flex-col h-full">
      <div
        ref={containerRef}
        className="flex-1 overflow-y-auto p-4 space-y-4"
      >
        <MessageList messages={messages} />
        {isStreaming && <StreamingNarration text={streamingText} />}
      </div>
      <div className="border-t border-ink p-4">
        <MessageInput onSend={handleSend} disabled={!connected || isStreaming} />
      </div>
    </div>
  )
}
```

#### 2. 消息列表

```tsx
// src/components/chat/MessageList.tsx

import type { ChatMessage } from '../../stores/chatStore'

interface MessageListProps {
  messages: ChatMessage[]
}

export function MessageList({ messages }: MessageListProps) {
  return (
    <>
      {messages.map((message) => (
        <MessageBubble key={message.id} message={message} />
      ))}
    </>
  )
}

function MessageBubble({ message }: { message: ChatMessage }) {
  const isUser = message.type === 'user'
  const isSystem = message.type === 'system'

  return (
    <div
      className={`
        max-w-[80%] p-3 rounded-lg
        ${isUser ? 'ml-auto bg-primary-500 text-white' : ''}
        ${message.type === 'dm' ? 'mr-auto bg-white border border-ink' : ''}
        ${isSystem ? 'mx-auto bg-gray-200 text-gray-600 text-sm italic' : ''}
      `}
    >
      {message.type === 'dm' && (
        <div className="font-display font-bold text-ink mb-1">DM</div>
      )}
      <div className="whitespace-pre-wrap">{message.content}</div>
      <div className="text-xs opacity-50 mt-1">
        {new Date(message.timestamp).toLocaleTimeString()}
      </div>
    </div>
  )
}
```

#### 3. 流式叙述

```tsx
// src/components/chat/StreamingNarration.tsx

interface StreamingNarrationProps {
  text: string
}

export function StreamingNarration({ text }: StreamingNarrationProps) {
  return (
    <div className="mr-auto max-w-[80%] p-3 rounded-lg bg-white border border-ink">
      <div className="font-display font-bold text-ink mb-1">DM</div>
      <div className="whitespace-pre-wrap">
        {text}
        <span className="animate-pulse">▌</span>
      </div>
    </div>
  )
}
```

#### 4. 消息输入

```tsx
// src/components/chat/MessageInput.tsx

import { useState, KeyboardEvent } from 'react'
import { Button } from '../ui/Button'

interface MessageInputProps {
  onSend: (text: string) => void
  disabled?: boolean
}

export function MessageInput({ onSend, disabled }: MessageInputProps) {
  const [text, setText] = useState('')

  const handleSend = () => {
    if (text.trim() && !disabled) {
      onSend(text.trim())
      setText('')
    }
  }

  const handleKeyDown = (e: KeyboardEvent<HTMLTextAreaElement>) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault()
      handleSend()
    }
  }

  return (
    <div className="flex gap-2">
      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        onKeyDown={handleKeyDown}
        placeholder="输入你的行动..."
        disabled={disabled}
        className="flex-1 px-3 py-2 border-2 border-ink rounded resize-none
                   bg-white text-ink font-body
                   focus:outline-none focus:ring-2 focus:ring-primary-300
                   disabled:opacity-50"
        rows={2}
      />
      <Button onClick={handleSend} disabled={disabled || !text.trim()}>
        发送
      </Button>
    </div>
  )
}
```

### 预期效果

```
┌─────────────────────────────────────────┐
│                                         │
│  DM: 欢迎来到费伦大陆...                 │
│                                         │
│              我想创建一个精灵法师         │
│                                         │
│  DM: 好的，让我帮你创建一个精灵法师...   │
│      首先我们需要确定属性...▌            │
│                                         │
├─────────────────────────────────────────┤
│  [输入你的行动...]              [发送]  │
└─────────────────────────────────────────┘
```

### 验收标准

```bash
# 1. 消息发送测试
# - 输入文字
# - 点击发送
# - 验证消息显示在列表中

# 2. 流式响应测试
# - 发送消息
# - 观察 DM 回复逐字显示

# 3. 快捷键测试
# - 按 Enter 发送
# - 按 Shift+Enter 换行
```

### 文档资料

- [Chat 组件设计](../modules/frontend/components/chat-components.md)

---

## 迭代 0.2.3 - 主界面布局

### 需求清单

- [x] 三栏式布局
- [x] 响应式适配
- [x] 面板切换

### 实现指导

#### 1. 主布局组件

```tsx
// src/components/layout/MainLayout.tsx

import { ReactNode } from 'react'
import { useUIStore } from '../../stores/uiStore'

interface MainLayoutProps {
  leftPanel?: ReactNode
  mainContent: ReactNode
  rightPanel?: ReactNode
}

export function MainLayout({ leftPanel, mainContent, rightPanel }: MainLayoutProps) {
  const { isPanelOpen, togglePanel } = useUIStore()

  return (
    <div className="h-screen flex flex-col">
      {/* Header */}
      <header className="h-12 bg-ink text-parchment flex items-center justify-between px-4">
        <h1 className="font-display text-xl font-bold">D&D AI DM</h1>
        <div className="flex items-center gap-4">
          <button
            onClick={togglePanel}
            className="hover:text-primary-300 transition-colors"
          >
            {isPanelOpen ? '◀ 隐藏面板' : '▶ 显示面板'}
          </button>
        </div>
      </header>

      {/* Main Content */}
      <div className="flex-1 flex overflow-hidden">
        {/* Left Panel */}
        {leftPanel && isPanelOpen && (
          <aside className="w-72 border-r border-ink overflow-y-auto">
            {leftPanel}
          </aside>
        )}

        {/* Center */}
        <main className="flex-1 overflow-hidden">
          {mainContent}
        </main>

        {/* Right Panel */}
        {rightPanel && (
          <aside className="w-64 border-l border-ink overflow-y-auto">
            {rightPanel}
          </aside>
        )}
      </div>
    </div>
  )
}
```

#### 2. 游戏页面

```tsx
// src/pages/GamePage.tsx

import { WebSocketProvider } from '../contexts/WebSocketContext'
import { MainLayout } from '../components/layout/MainLayout'
import { ChatContainer } from '../components/chat/ChatContainer'
import { CharacterPanel } from '../components/panels/CharacterPanel'
import { QuickActions } from '../components/panels/QuickActions'
import { ConnectionStatus } from '../components/ui/ConnectionStatus'

function GamePageContent() {
  return (
    <MainLayout
      leftPanel={<CharacterPanel />}
      mainContent={<ChatContainer />}
      rightPanel={<QuickActions />}
    />
  )
}

export default function GamePage() {
  return (
    <WebSocketProvider>
      <ConnectionStatus />
      <GamePageContent />
    </WebSocketProvider>
  )
}
```

#### 3. 角色面板（简化版）

```tsx
// src/components/panels/CharacterPanel.tsx

import { useGameStore } from '../../stores/gameStore'
import { Panel } from '../ui/Panel'

export function CharacterPanel() {
  const party = useGameStore((s) => s.gameState?.party)

  if (!party || party.length === 0) {
    return (
      <Panel title="角色">
        <p className="text-gray-500 text-sm">尚未创建角色</p>
      </Panel>
    )
  }

  return (
    <div className="p-4 space-y-4">
      {party.map((character) => (
        <Panel key={character.id} title={character.name}>
          <div className="space-y-2">
            <div className="text-sm">
              {character.race} {character.class} Lv.{character.level}
            </div>

            {/* HP Bar */}
            <div className="space-y-1">
              <div className="flex justify-between text-xs">
                <span>HP</span>
                <span>{character.currentHitPoints}/{character.maxHitPoints}</span>
              </div>
              <div className="h-2 bg-gray-200 rounded overflow-hidden">
                <div
                  className="h-full bg-red-500 transition-all"
                  style={{
                    width: `${(character.currentHitPoints / character.maxHitPoints) * 100}%`,
                  }}
                />
              </div>
            </div>

            {/* AC */}
            <div className="flex justify-between text-sm">
              <span>AC</span>
              <span className="font-bold">{character.armorClass}</span>
            </div>

            {/* Attributes */}
            <div className="grid grid-cols-3 gap-1 text-xs">
              {Object.entries(character.abilityScores).map(([ability, value]) => (
                <div key={ability} className="text-center p-1 bg-gray-100 rounded">
                  <div className="font-bold uppercase">{ability.slice(0, 3)}</div>
                  <div>{value} ({Math.floor((value - 10) / 2):+d})</div>
                </div>
              ))}
            </div>
          </div>
        </Panel>
      ))}
    </div>
  )
}
```

#### 4. 快捷操作面板

```tsx
// src/components/panels/QuickActions.tsx

import { Button } from '../ui/Button'
import { Panel } from '../ui/Panel'

export function QuickActions() {
  return (
    <div className="p-4">
      <Panel title="快捷操作">
        <div className="space-y-2">
          <Button variant="secondary" className="w-full">
            🎲 检定
          </Button>
          <Button variant="secondary" className="w-full">
            📦 背包
          </Button>
          <Button variant="secondary" className="w-full">
            🔮 法术
          </Button>
          <Button variant="secondary" className="w-full">
            💾 存档
          </Button>
        </div>
      </Panel>
    </div>
  )
}
```

### 预期效果

```
┌──────────────────────────────────────────────────────────────────────┐
│  D&D AI DM                                     ◀ 隐藏面板            │
├──────────────┬────────────────────────────────────┬──────────────────┤
│              │                                    │                  │
│  角色面板    │        聊天区域                    │   快捷操作       │
│              │                                    │                  │
│  艾拉        │  DM: 欢迎来到费伦...               │   [🎲 检定]      │
│  精灵法师    │                                    │   [📦 背包]      │
│  Lv.1        │        我想创建角色                │   [🔮 法术]      │
│  HP: 6/6     │                                    │   [💾 存档]      │
│  AC: 12      │  DM: 好的...▌                      │                  │
│              │                                    │                  │
├──────────────┴────────────────────────────────────┴──────────────────┤
│  [输入你的行动...]                                       [发送]       │
└──────────────────────────────────────────────────────────────────────┘
```

### 验收标准

```bash
# 1. 布局渲染测试
# - 各区域正确显示
# - 面板可切换

# 2. 响应式测试
# - 不同窗口尺寸下布局正确
```

### 文档资料

- [UI 模块设计](../modules/frontend/ui-module.md)

---

## v0.1 集成验收

### 端到端测试场景

```
1. 启动后端服务（端口 8080）
2. 启动前端开发服务器（端口 5173）
3. 浏览器访问 http://localhost:5173
4. 点击"开始游戏"
5. 进入游戏界面
6. 检查 WebSocket 连接状态（应显示"已连接"）
7. 在聊天框输入："我想创建一个精灵法师"
8. 观察流式 DM 叙述
9. 观察角色面板更新
10. 刷新页面，验证状态恢复
```

### 发布标准

- [ ] 前端可启动并显示界面
- [ ] WebSocket 可连接后端
- [ ] 用户输入可发送
- [ ] 流式叙述可显示
- [ ] 基础布局正常工作
- [ ] 状态可持久化

---

---

## 迭代 0.2.4 - 角色面板基础（F007）

### 需求清单

- [x] 角色卡片（名称、种族、职业、等级）
- [x] 六大属性值与修正显示
- [x] HP / AC 展示
- [x] 订阅 gameStore 实时更新

### 实现指导

#### 1. CharacterPanel 组件

```tsx
// src/components/panels/CharacterPanel.tsx
import { useGameStore } from '@/stores/gameStore'

export function CharacterPanel() {
  const characters = useGameStore((s) => s.characters)
  const party = useGameStore((s) => s.party)

  if (!party || party.length === 0) {
    return <Panel title="角色"><p className="text-gray-400">尚未创建角色</p></Panel>
  }

  return (
    <Panel title="队伍">
      {party.map((charId) => {
        const char = characters[charId]
        return char ? <CharacterCard key={charId} character={char} /> : null
      })}
    </Panel>
  )
}
```

#### 2. CharacterCard 组件

```tsx
// src/components/panels/CharacterCard.tsx
interface CharacterCardProps {
  character: Character
}

export function CharacterCard({ character }: CharacterCardProps) {
  const abilities = ['str', 'dex', 'con', 'int', 'wis', 'cha'] as const
  const abilityLabels = { str: '力量', dex: '敏捷', con: '体质', int: '智力', wis: '感知', cha: '魅力' }

  return (
    <div className="bg-gray-800 rounded-lg p-3 mb-2 border border-gray-700">
      {/* 头部 */}
      <div className="flex justify-between items-start mb-2">
        <div>
          <h3 className="font-bold text-white">{character.name}</h3>
          <p className="text-sm text-gray-400">{character.race} {character.class} Lv.{character.level}</p>
        </div>
        <div className="text-right">
          <div className="text-sm text-gray-400">AC</div>
          <div className="font-bold text-yellow-400">{character.armorClass}</div>
        </div>
      </div>

      {/* HP 条 */}
      <HealthBar current={character.currentHitPoints} max={character.maxHitPoints} />

      {/* 属性值（简洁版） */}
      <div className="grid grid-cols-6 gap-1 mt-2">
        {abilities.map((ability) => {
          const score = character.abilityScores[ability] ?? 10
          const mod = Math.floor((score - 10) / 2)
          return (
            <div key={ability} className="text-center bg-gray-700 rounded p-1">
              <div className="text-xs text-gray-400">{abilityLabels[ability].slice(0, 2)}</div>
              <div className="text-sm font-bold text-white">{score}</div>
              <div className="text-xs text-gray-300">{mod >= 0 ? `+${mod}` : mod}</div>
            </div>
          )
        })}
      </div>
    </div>
  )
}
```

#### 3. HealthBar 组件

```tsx
// src/components/ui/HealthBar.tsx
interface HealthBarProps {
  current: number
  max: number
}

export function HealthBar({ current, max }: HealthBarProps) {
  const pct = max > 0 ? Math.max(0, Math.min(100, (current / max) * 100)) : 0
  const color = pct > 50 ? 'bg-green-500' : pct > 25 ? 'bg-yellow-500' : 'bg-red-500'

  return (
    <div className="flex items-center gap-2">
      <div className="flex-1 bg-gray-600 rounded-full h-2">
        <div className={`${color} h-2 rounded-full transition-all`} style={{ width: `${pct}%` }} />
      </div>
      <span className="text-xs text-gray-300 whitespace-nowrap">{current}/{max}</span>
    </div>
  )
}
```

#### 4. gameStore 扩展（角色字段）

```typescript
// src/stores/gameStore.ts - 追加角色相关 state
interface GameStore {
  characters: Record<string, Character>  // id -> Character
  party: string[]                         // 队伍角色 ID 列表
  setCharacter: (char: Character) => void
  updateCharacterHP: (id: string, current: number) => void
}

// 处理 state_update 消息中的角色数据
// type: 'character_update', data: Character
```

### 集成到主布局

```tsx
// src/components/layout/GameLayout.tsx
<aside className="w-64 flex flex-col gap-2 overflow-y-auto">
  <CharacterPanel />  {/* F007 */}
</aside>
```

### 验收测试

```
1. 创建角色后，面板自动显示角色信息
2. 角色 HP 变化时，HP 条实时更新
3. 六大属性修正值计算正确（属性16 → +3）
4. AC 显示正确
```

---

**文档版本**: v1.1
**创建日期**: 2026-03-18
**更新日期**: 2026-03-18
**作者**: 开发计划师
