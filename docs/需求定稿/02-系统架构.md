# 系统架构

> [返回索引](./index.md)

---

## 2.1 系统组成

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户层                                      │
│              [Web 前端界面：自然语言输入/地图交互/战斗操作]              │
└─────────────────────────────────────────────────────────────────────────┘
                    │                                    │
                    │ 用户操作                            │ 状态展示
                    ▼                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       游戏后端 (Backend)                                 │
│      (Client + Server 合并部署)                                          │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                     Client 模块                                    │  │
│  │  ┌───────────┐    ┌───────────┐    ┌───────────────────────┐      │  │
│  │  │ 会话管理  │◀───│上下文管理 │◀───│ LLM 适配层            │      │  │
│  │  └───────────┘    └───────────┘    └───────────────────────┘      │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                              │ MCP 格式接口                              │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                     Server 模块 (D&D 5e 规则引擎)                  │  │
│  │  会话管理 │ 角色系统 │ 战斗系统 │ 检定/掷骰 │ 背包/物品 │ 法术系统  │  │
│  │  剧本系统 │ 地图系统 │ 状态效果                                          │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │   共享数据 (游戏状态/会话历史/存档) + 持久化层                      │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                    │ API 请求
                    ▼
            ┌───────────────┐
            │     LLM       │
            │ (DM + NPC控制)│
            └───────────────┘
```

---

## 2.2 交互关系

**数据流向说明**：

1. **用户 → Web 前端**：用户通过 Web 界面进行输入（自然语言/地图点击/战斗操作）
2. **Web 前端 → 游戏后端**：通过 **WebSocket** 将用户操作传递给 Client 模块处理
3. **Client → LLM**：Client 模块将用户输入发送给 LLM 进行理解和决策
4. **LLM → Client**：LLM 返回叙述内容或工具调用指令
5. **Client → Server**：通过 MCP 格式接口调用 Server 模块工具（内部调用，无网络开销）
6. **Server → Client**：返回执行结果，更新共享游戏状态
7. **游戏后端 → Web 前端**：通过 **WebSocket** 推送状态更新、战斗事件、执行过程到 Web 前端展示
8. **Web 前端 → 用户**：可视化展示（React UI + Phaser Canvas 渲染地图、战斗动画、叙述文本等）

---

## 2.3 技术栈

| 组件 | 技术选型 |
| --- | --- |
| **后端语言** | Go |
| **前端 UI** | React |
| **前端游戏渲染** | Phaser |
| **通信协议** | WebSocket |
| **内部通信** | MCP 格式（合并部署） |
| **地图格式** | FVTT |

---

> [上一章：项目概述](./01-项目概述.md) | [下一章：功能需求](./03-功能需求.md)
